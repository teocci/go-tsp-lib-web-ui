export default class MenuPanel {
    constructor(placeholder, points) {
        // Use this class to control the toggle switches to control if you
        // show or not the executed routes.
        this.placeholder = placeholder
        this.points = points
    }
}